{
  "name": "automation-wizard-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -F @auto-wiz/extension dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "release": "changeset publish"
  },
  "packageManager": "pnpm@9.0.0",
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "playwright": "^1.40.0"
  },
  "dependencies": {
    "@typescript/native-preview": "7.0.0-dev.20251213.1"
  }
}
